{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the app windows opened from the tray",
  "windows": ["custom-pause", "main", "settings", "stats"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
